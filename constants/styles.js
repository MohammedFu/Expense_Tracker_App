export const GlobalStyles = {
  colors: {
    // Primary (based on #6b9080 – muted green-teal)
    primary50: '#f0f5f3',  // Lightest background tint
    primary100: '#d6e4e0', // Soft fill for cards
    primary200: '#a8c5bd',  // Subtle borders
    primary400: '#6b9080',  // Your base color (buttons, accents)
    primary500: '#4a7a6b',  // Saturated for active states
    primary700: '#2d5c50',  // Darker for text/headings
    primary800: '#1a3a32',  // Darkest (e.g., app bar)

    // Accent (warm contrast to green-teal)
    accent500: '#e9c46a',  // Gold-yellow (replaces #f7bc0c)

    // Error (softened red-pink)
    error50: '#ffebee',
    error500: '#e57373',  // Muted coral-red

    // Grays (cool, blends with green)
    gray500: '#5a6d7a',  // Medium gray (text)
    gray700: '#3a4a53',  // Dark gray (headings)
  },
};